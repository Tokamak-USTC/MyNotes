### CRTP

1. CRTP是静态多态的一种
2. 基类实现方式：
	- 基类是以子类类型为 **模版参数** 的 **模版类**
	- 通过在统一接口中使用 `static_cast` 强转 `this` 指针来调用子类接口

```cpp
template<class Derived>
class Base {
public:
	void interface() {
		static_cast<Derived*>(this)->implementation();
	}
};
```

3. 子类实现方式：
	- 子类是**普通类**
	- 子类继承 **以自身类型为模版参数** 实例化的基类
	- 子类必须实现 implementation，否则会编译期报错（基类强转指针之后找不到 implementation 的实现

```cpp
class DerivedA : public Base<DerivedA> {
public:
	void implementation() {
		std::cout << "  DerivedA::implementation\n";
	}
}
```

4. 和虚函数相比的优点：
	- **零开销** 多态，不需要间接调用，没有运行时分发开销，可以把具体实现给 **内联** 了。
5. 和虚函数相比的缺点：
	- 无法实现真正的 **运行时多态**，不能通过基类指针 / 引用来做运行时分发。
	- 一个 `Derived` 对应一个 `Base<Derived>` 实例，可能造成代码膨胀。
6. CRTP适合 **代码复用** / **流程复用**，比如模版方法模式，以及对性能敏感的场景

### std::string_view

1. 是什么：
	- C++17 引入的 **只读、非拥有字符视图**，内部一般只存 `const char*` 指针 + `size_t` 长度，零堆分配、零拷贝。
2. 解决什么痛点：
	- 替代 `const std::string&`：传字面量时不需要先 **隐式构造** 临时 `std::string`
	- 替代 `const char*`：不仅有指针，还有长度信息
	- 统一接口：可以用一个参数类型同时接收字面量、`std::string`、`const char* + 长度`，避免不必要的隐式拷贝
3. 核心优势：
	- 构造 / 拷贝极轻量
	- `substr()` 是 **零拷贝** 的，返回的仍然是 view
	- 能统一接收多种来源的字符串，而不必都转成新的 `std::string`
4. 最大陷阱——悬空：
	- 它 **不拥有** 内存，底层字符数据必须由别人保证生命周期
	- 如果指向局部变量或临时 `std::string`，原对象析构后就会变成悬空引用，继续使用是未定义行为
	- 如果原 `std::string` 发生扩容 / 重分配，已有 `string_view` 也可能失效
5. 其他注意事项：
	- 不保证 `\0` 结尾，不能直接当 C 字符串用
	- 永远只读，不能通过 `string_view` 修改字符
	- `std::string_view::substr()` 和 `std::string::substr()` 不同，前者零拷贝，后者会构造新的 `std::string`
6. 最佳实践：
	- 函数参数、局部只读切片适合用 `std::string_view`
	- 返回值和类成员变量优先用 `std::string`，避免生命周期问题

### 让对象只能构造在栈 / 堆上

1. 只能构造在栈上：
	- 删除 `operator new(std::size_t)` 和 `operator new[](std::size_t)` ，来禁止直接使用 `new` 来分配内存
	- 删除 `operator new(std::size_t, void*)`，来**禁止使用 `new(p) Foo()` 这种placement new的方式构造元素**
	- 使用 `::operator new(std::size_t)` 分配堆内存再把对象拷贝到堆内存上不能通过这种方法避免，`std::vector` 等容器也会把对象放在堆上

```cpp
class StackOnly {
public:
  StackOnly() = default;
  void *operator new(std::size_t) = delete;
};

int main() {
  StackOnly s;                    // OK
  StackOnly *p = new StackOnly(); // Call to deleted function 'operator new'
}
```

2. 只能构造在堆上：
	- **析构函数私有化**：栈对象离开作用域时必须调用析构函数，析构函数私有化后，编译器在编译期就会发现**析构函数不可访问**，于是**直接拒绝该对象的创建**
	- 如果析构函数私有化，即便工厂函数是创建的栈上对象，外部调用者也无法调用，因为**返回值会在调用者的作用域析构**，而这样就会在类外部调用析构函数从而导致编译报错
	- 如果某个成员函数内部创建栈对象并且**作用域内使用且不返回**，那仍然会在栈上创建对象
	- 可以在栈上预留空间并直接把堆上对象拷贝到栈上

```cpp
class HeapOnly {
public:
  static HeapOnly *create() { return new HeapOnly(); }
  void destory() { delete this; }

private:
  ~HeapOnly() = default;
};

int main() {
  HeapOnly h; // Variable of type 'HeapOnly' has private destructor

  HeapOnly *p = HeapOnly::create(); // OK
  delete p;     // Calling a private destructor of class 'HeapOnly'
  p->destory(); // OK

  alignas(HeapOnly) unsigned char buf[sizeof(HeapOnly)];
  new (&buf) HeapOnly(); // OK
}
```

### static 关键字

#### 修饰变量

1. 静态全局变量：静态存储期，内部链接，当前文件内可见
2. 静态局部变量：**静态存储期**（程序整个生命周期都存在），只在函数内可见，**第一次访问时初始化**，可以控制初始化顺序。从C++11开始，**局部静态变量的初始化是线程安全的**，可以用来实现**单例模式**
3. 静态成员变量：静态存储期，类内可见，以 `Foo::x` 的形式访问，C++17之前需要在源文件内显式定义，C++17之后可以用 `inline static` 

#### 修饰函数

1. 静态全局函数：内部链接，只能在当前文件内调用
2. 静态成员函数：属于类，无 `this` 指针，调用时不需要对象，**只能访问类的static成员**

### 单例模式

C++11之后的推荐写法是使用静态局部变量的Meyers Singleton

```cpp
class Singleton {
public:
  static Singleton &instance() {
    static Singleton instance;
    return instance;
  }
  
private:
  Singleton() = default;
  ~Singleton() = default;
  Singleton(const Singleton &) = delete;
  Singleton &operator=(const Singleton &) = delete;
};
```

### mutable 关键字

1. 修饰**非静态成员变量**，使其可以**被const成员函数修改**
2. 修饰**lambda表达式**，使其可以修改 **按值捕获** 的变量

```cpp
void test() {
  int a = 0;
  auto lambda0 = [a]() {
    a++; // Cannot assign to a variable captured by copy in a non-mutable lambda
  };
  auto lambda1 = [a]() mutable { a++; };
  lambda1(); // a = 1
  lambda1(); // a = 2
}
```

