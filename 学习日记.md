
## 9.1

1. 流水线CPU的指令退休阶段 / ROB / 架构寄存器文件 / 物理寄存器文件 / 寄存器重命名消除假依赖
2. concept 和 enable_if(SFINAE)
3. 对象内存模型 / static_cast / dynamic_cast / RTTI
4. dpdk基本原理 / 大页内存 / mbuf / 锁页 / 用户态 DMA
	- TLB压力小
	- page walk次数少 -> 2M大页 walk 到 PMD即可
	- 物理内存连续，DMA设备访问效率高
5. linux 进程调度 / struct task / sched_entity / vruntime / 红黑树挂树

## 9.2

1. 腐烂的橘子
2. 课程表
3. 图的表示 / 链式前向星
	- head数组 / edge数组
	- head数组下标是顶点id，内容是第一条出边在edge数组中的下标
	- edge数组第一个元素是终点在head数组的下标，第二个元素是同起点的 **下一条出边** 在edge数组下标，如果是最后一条出边则为 **-1**
	- 遍历时使用一个bool数组vis来记录哪些边遍历到了