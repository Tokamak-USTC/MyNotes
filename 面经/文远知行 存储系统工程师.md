## 一面 7.13

1. 自我介绍，介绍了EIC的一些相关内容（业务背景，部署方式，设计取舍），面试官对训推一体的设计架构提出疑问，后来澄清是共用物理集群，划分不同的逻辑域来存储文件和kv，client同时提供文件/kv语义的接口。
2. 项目经历，主要讲了缩容迁移加速的项目（个人感觉讲的不够清楚稍有些乱），提问整个集群的规模有多大
3. 做题
	- [20. 有效的括号](https://leetcode.cn/problems/valid-parentheses/)，使用栈 `std::stack` 解决
	- [23. 合并 K 个升序链表](https://leetcode.cn/problems/merge-k-sorted-lists/)，使用堆 `std::priority_queue` 解决，问时间复杂度：O(mnlog(m))
		- 坑点：`std::priority_queue` 的 `top()` 返回的是**常引用**，`pop()` 之后才能修改。面试用通过 wrapper 最终绕过了这个坑
		- 问题出在为了方便比较，直接让优先队列存了链表节点
		- 一般是先直接**拷贝**top然后pop掉，再修改拷贝的副本
4. 反问
  - 问对面试官个人对候选人的要求：口水话。
  - 问对ai时代对面试考察手撕/算法题的看法：还是比较重要的，反映个人能力。
  - 问对ai使用的态度：比较保守，ai写的代码会仔细review。
  - 交流了一些对ai使用/ai提效的看法和经验。

关于 `std::priority_queue` 使用，见[[排序和比较]]。

## 二面 7.15

1. 简单自我介绍
2. 项目一n2n迁移缩容介绍。这次故事讲的比上次清晰一些了。问题：
	- 如果某个bg的两个副本都在缩容的5%节点里怎么办 -> 澄清选源机制，这两个节点自己搬这个bg，其他bg可以根据迁移负载来选其他的节点作为源
	- 缩容时间是硬性约束吗？-> 是，如果时间到了就kill进程硬下线了，dn变ABNORMAL，bg降级为单副本状态，通过replicate补齐
	- 一个节点大概有多少数据需要迁出？-> 单盘2.1T，单dn三块盘
	- 缩容的节点如果被作为目的端怎么办？-> diskpicker硬性约束，四种状态不能作为目的节点，在飞的通过巡检保证最终搬走
	- 这个功能上线多久了？出过什么问题吗？-> 3月中上线，线上没有出过什么调度侧问题，一般都是执行侧阻塞
3. 项目二迁移清理介绍。背景 + 实现细节 + 特殊考虑（路由版本约束）
4. 这些项目你参与了哪些部分？-> 控制面的工作都是独立完成的，数据面的轻量适配由同事完成
5. 线上出过什么问题吗？介绍一次定位经历？-> 还挺多的，但是都没啥特别的印象不太深，要不我打开飞书给您看看？（笑）主要就是根据故障时间去物理机查日志，看哪些副本残留阻塞了状态流转，找到巡检这些副本，尝试生成任务的日志行，看看是执行侧阻塞了还是因为单飞限制阻塞
6. 做题
	- [146. LRU缓存](https://leetcode.cn/problems/lru-cache/description/)，使用哈希表 `std::unordered_map` 和双向环形链表解决，时间复杂度插入删除都是 O(1)。时间不够没有把插入结构实现完整，用注释写了思路。
7. 反问
	- 对手撕算法题的看法。-> 看候选人有没有一些common sense，比如你做缓存，应该了解LRUCache相关的东西，比如你用C++，那你应该对C++的数据结构有些了解。总之还是看你是否聪明，来了之后能不能高效合作。
	- 如果我没做出来这题，你打算怎么办？-> 那就换一题
	- 把面试当成任务吗，还是也愿意来看看这一届的候选人什么水平？->（笑）不是二极管的，提前看简历，如果对项目有兴趣也会多聊聊
	- 组里也是做C++吗 -> 是的，文件存储C++，对象存储go
	- 问我，既然你觉得算法题用不到，那初中高中学的那些只是也考过就扔，你觉得意义何在？-> 我觉得更多的是筛选人的，通过考试强行给人划分等级，很畸形，不利于人的长远发展