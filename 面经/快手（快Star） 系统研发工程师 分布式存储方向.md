## 一面 7.30

1. 自我介绍
2. map相关问题：
	- map怎么实现的？-> 红黑树 / 哈希表
	- 怎么实现一个线程安全且读写性能高的map?
		- 读写加锁保证线程安全
		- 使用读写锁保证可以并发读
		- RCU可以缩短临界区，让读写互不影响
		- 数据互不影响时，可以对数据进行分桶（细粒度锁）
3. 任务队列相关：
	- 怎么实现一个长度固定的任务队列？-> ring buffer，维护头指针和尾指针，留一个slot判断队满/队空
	- 如果写线程发现堆满了，如何处理？
		- 无锁队列场景可以使用yield来让出线程
		- 正常可以使用条件变量，发现队满休眠等待，读线程消费掉数据之后唤醒
4. EIC基本架构介绍：
	- 核心组件：client、rootserver、danceDN
	- RS核心概念：blockgroup，视图，迁移，路由分发
	- 你们的文件目录树的meta在哪里存？-> namenode，我负责的偏推理，对这个组件了解不深
5. 项目一P2P缩容迁移加速介绍：
	- 项目背景，P2P任务分发，负载均衡选源，最终收益
	- 你们是如何保证副本和副本之间的一致性的？-> 不是强一致，正常情况下通过链式写，迁移期间双写，来保证副本内容一致。顺带讲了迁移任务的大致过程，副本快照/双写机制
	- 为什么前台IO成功率降低 -> ktcp抢占机器的cpu资源
	- 为什么OOM风险加大 -> 推测是DN侧排队了。然后面试官说可能并发迁移也会导致
6. 项目二清理系统：
	- 面试官水平很高，看简历描述的内容已经把项目推测了个大概，反着给我介绍了一遍
	- 讲了一下清理失效的风险，就是集群雪崩，面试官说确实是个正反馈，不过应该没有那么快。我是说线上问题文档里使用的是这个口径
7. 项目三xv6操作系统：
	- 讲一下物理内存分配和虚拟内存分配
		- xv6的物理内存维护一个全局空闲链表，通过一个 kmalloc接口分配，从空闲链表摘一块，然后分配
		- 虚拟内存是用kmalloc分配好物理内存，然后page walk找到页表里的对应位置，然后做好映射
		- 顺便提了一下linux的vma管理，maple tree这些概念
	- 说一下C++的数据都在内存的什么位置：
		- 很粗略的讲了一下ELF的结构，装载视角。时间不够没讲特清楚，提到了.data/.bss这种section会被拼成一个segment，准备那些ASLR之类的装逼内容都没来得及讲
	- 你提到了PIE，那你说说动态链接和静态链接有什么不同
		- 编译期，要加-fPIC这个参数来编译位置无关代码，主要体现在留下的重定位条目的修补方式不同。
		- 链接期，会生成.plt这种代码桩结构用来间接跳转，防止运行时对.text的代码进行修补
		- 还有一些延迟绑定之类的特性还没展开讲
8. 做题：
	- 第一题，二叉树后序遍历非递归 -> 直接说我记不住，如果有充足的时间能磨出来，但是现在时间紧张可能难做出来
	- 然后说没关系，咱们换个简单的，[19. 删除链表的倒数第 N 个结点](https://leetcode.cn/problems/remove-nth-node-from-end-of-list/) -> 双指针 + dummy head，看了一眼说没问题
9. 反问：
	- 组立是做什么的？-> 存储大组从存储底座到块/文件都做，他们主要做文件，peer组是缓存存储，对接AI场景
	- 那你们是主要管训练吗？-> 是的
	- 表示这是我面这么多场里信息密度最高的一场 -> 感谢你的分享 -> 也感谢面试官的时间

## 二面 8.5

1. 面试官是我mt之前的同事还是科大老学长，以前在fsx对接EIC，对老架构很熟悉，直接就问团队的情况和mt的情况，自我介绍都省下了
2. 让我介绍项目的新架构，问了一些问题
	- 新架构怎么应对扩容时候的副本分发场景的 -> 通过rs的balance调度
	- 新架构怎么单value热点问题的 -> value切成block打散到多个DN上
3. 讲讲P2P迁移选源
	- 为什么你们的balance不把物理源副本和逻辑源副本解耦 -> 设计的问题，做的粗糙，我是想统一成一种通用任务类型的 
	- 统一了有什么好处和坏处？-> 
		- **balance场景下，高水位的DN可能承担更多的前台IO，就不太应该让他们叠加后台迁移流量了**
		- 统一之后代码好维护
		- 但是后面为了解决扩容均衡速度慢的问题，balance补了一大堆的特殊的调度层补丁，现在基本已经没法统一了
	- 介绍了一下双写，问路由版本落后怎么办 -> 落后就落后了，没办法，本来也不是强一致系统，满足SLA就行了 
		- 说你们做kv cache就这点爽，不用管同步不同步的
		- 那如果让你给做一致，你怎么保证呢？-> client读写带上路由版本，DN进行校验，不匹配则请求失败？-> 也行吧
		- 跟我现场讲了ceph的迁移期路由同步机制，听了个大概没听太懂
	- 说你这个事情我也做过
4. 讲讲路由压缩缓存
	- 你怎么知道瓶颈就一定在序列化上，而不再网络带宽上 -> perf top看出来的
	- 你们的一个路由的网络包有多大 -> 6k节点 100W BG大概 四十多M
	- 那你们rs的出带宽有多大，哪怕你们100的QPS，这么大的网络包都要4G的出带宽了 -> 不知道具体多大，但是我们还有压缩来解决网络包的大小
	- 增量版本历史有多少？增量路径带宽能扛住吗 -> 可以配，一般5W 个BG，比全量小很多了
	- 最后提了一嘴zstd，8倍的压缩比，勉勉强强说服了这个能解决掉出带宽的问题
5. 讲讲迁移残留清理
	- DN能拿到路由的版本，他自己知道某个BG在不在自己上面，为啥不让DN自己根据这个路由版本去做清理？
		- 我说路由版本不可靠有延迟，RS的迁移任务是在路由版本更新前调度的，怕误删数据
		- 那你们的迁移任务里面没有带版本号吗？DN收到任务之后可以等路由版本更新了再开始执行啊？
		- 我说这个确实是办法，但是一开始还没有做这个，后来加上的。（其实我下来查了代码，到现在还没做这个保护呢）
		- 感觉面试官有点无语了，问我你们这RS是谁做的啊？-> 一开始是xiaoyong，后来是我，zuocheng，shili我们仨人在搞
6. 问点基础知识
	- 讲讲io_uring吧 -> 有个sq, 有个cq, 用户包装一些任务(**sqe**)填到sq里，内核通过轮询收割然后执行，（这是SQPOLL模式，默认需要用户通过系统调用批量提交sqe)，执行完之后填写cqe放入cq，然后用户进行进一步处理
	- rdma呢？-> 我说我其实不太了解，但是感觉和io_uring思路类似，也是俩队列，然后需要锁页内存防止换出，然后网卡直接去读取这些pin住的内存往远端打。
		- 那你用户锁的内存是虚拟内存，网卡怎么知道虚拟内存对应的物理内存在哪的？-> 我猜测，用户让rdma网卡干活肯定要经过内核，那内核就可以进行一个page walk去把虚拟地址翻译成物理地址然后告诉网卡 -> 差不多吧
	- mmap说说 -> 讲了vma, maple tree那些东西
7. 反问，聊了很久
	- 聊了对于存储领域的看法 -> 不如真的infra，但是可能比业务安全一点，如果有能力还是去做离ai更近的事情
	- 聊了对于候选人或者校招生的期待，有哪些纸面之外的要求 -> 校招生最主要的就是靠谱，有问题别自己憋着，多和同事对齐，不会的多去问
	- 问你觉得那种经历比较杂的和经历比较垂直的候选人在校招中区别大吗 -> 感觉区别还是有的，比如我看见你的简历肯定会觉得有意思
	- 问了名字和工作年限 -> youyi，干了十年左右，和我mt差不多
	- 问我你导师是谁 -> 硕士zhangshu，本科zhangxinming（这个认识）
	- 感谢面试官的时间

## 三面 8.12

1. 自我介绍
2. 入职的前几个月做了什么？
	- 前两个月搞client，指标系统，gdr路径crc计算
	- 后两个月搞可观测性，监控告警配置
3. EIC团队和产品演进
	- 训推一体合并形态的简单介绍
4. 有关注过对业务的影响吗？这个读的延迟会让TTFT上升吗 -> 我猜应该比计算开销小
	- 说其实不一定，可能延迟提升了，不过节省计算资源
5. gdr相关
	- 显存计算路径相比cpu计算路径有多大提升？-> 取决于IO pattern，大IO提升明显
	- 简单说了一下基本流程，业务分配显存，然后用IOBuf包起来，传给sdk
	- 那数据节点数据落盘这段时间，显存一直不能释放吗 -> 我说可以先写内存，然后内存写成功就返回resp，然后再链式写其他dn / 写ssd（其实不是这样的，确实是等一个节点落盘了才能返回成功）
	- 读路径怎么办？-> 读路径可能不用gdr吧，先读到内存再拷回去
6. 半托管相关
	- 为什么半托管？-> 成本问题，gpu机器的ssd给利用起来
	- 那为啥不给ssd把下来？-> 直接告诉我他也问过别人，是因为nvidia卖机器的时候有推荐配置，一个机器上就带几块ssd，但是ssd比gpu便宜太多了，所以也没人动过，怕乱动出问题（是不是怕不给保修了）
	- 半托管比拉远还有什么优势吗？-> 可能会好做一些本地亲和，网络开销更低
	- 本地亲和怎么做？-> 我说写本地 -> 告诉我应该是计算任务跟住存储调度
7. 介绍快手的基础架构发展情况
	- 表示团队虽然起步晚，但是能做的事情很多，对个人的成长很有好处
8. 没什么反问，表示会给我继续推进流程 -> 问了一下后续流程是什么，说可能是hr面，他也不太清楚，可以和hr沟通
## 三面（续）8.13

1. 说是公司需要知道候选人对ai coding的使用能力和看法，昨天忘了问了，就关于这个事情又很随便的聊了20min左右

## 四面 8.20

1. 面试官是快手基础平台的总负责人叶霖，说这次是定级面
2. 简单自我介绍
3. 介绍项目的基本架构和训练 / 推理场景的基本使用姿势和差异
4. 介绍Rootserver基本概念
5. 介绍缩容迁移项目
6. 介绍路由压缩项目
7. 介绍迁移清理项目
8. 简单八股，介绍一下网络的层次结构
9. 单线程和多线程有什么区别
10. 来不及反问了，感谢你的时间